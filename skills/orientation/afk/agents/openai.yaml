interface:
  display_name: "AFK"
  short_description: "The user explicitly invokes afk to continue a task unattended"
  default_prompt: "Use og:afk when the user explicitly invokes afk to continue a task unattended."
policy:
  allow_implicit_invocation: false
