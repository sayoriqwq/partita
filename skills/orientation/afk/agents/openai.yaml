interface:
  display_name: "AFK"
  short_description: "The user asks the agent to continue a task unattended"
  default_prompt: "Use og:afk when the user asks the agent to continue a task unattended."
policy:
  allow_implicit_invocation: true
