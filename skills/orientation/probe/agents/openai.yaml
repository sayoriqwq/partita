interface:
  display_name: "Probe"
  short_description: "The user explicitly asks to probe a load-bearing implementation uncertainty in the actual task environment"
  default_prompt: "Use og:probe when the user explicitly asks to probe a load-bearing implementation uncertainty in the actual task environment."
policy:
  allow_implicit_invocation: false
