interface:
  display_name: "Land"
  short_description: "The user explicitly invokes land to keep a first version or current topic from expanding before it is landed"
  default_prompt: "Use og:land when the user explicitly invokes land to keep a first version or current topic from expanding before it is landed."
policy:
  allow_implicit_invocation: false
