interface:
  display_name: "Why"
  short_description: "The user explicitly invokes why to reconstruct the Why behind one ADR"
  default_prompt: "Use og:why when the user explicitly invokes why to reconstruct the Why behind one ADR."
policy:
  allow_implicit_invocation: false
