interface:
  display_name: "Aim"
  short_description: "The user explicitly invokes aim to set, reset, sustain, or check the current conversation aim or request visible alignment against a current direction"
  default_prompt: "Use og:aim when the user explicitly invokes aim to set, reset, sustain, or check the current conversation aim or request visible alignment against a current direction."
policy:
  allow_implicit_invocation: false
