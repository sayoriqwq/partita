interface:
  display_name: "Reconcile"
  short_description: "The user explicitly invokes reconcile to recall a settled case and audit residue before cleanup"
  default_prompt: "Use mt:reconcile when the user explicitly invokes reconcile to recall a settled case and audit residue before cleanup."
policy:
  allow_implicit_invocation: false
