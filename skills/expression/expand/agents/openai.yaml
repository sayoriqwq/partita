interface:
  display_name: "Expand"
  short_description: "The user explicitly clarifies one concept with a concrete example"
  default_prompt: "Use ex:expand when the user explicitly clarifies one concept with a concrete example."
policy:
  allow_implicit_invocation: false
