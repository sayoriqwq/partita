interface:
  display_name: "Expand"
  short_description: "The user explicitly invokes expand to give concept A more source-grounded detail and context-fit examples for clearer explanation"
  default_prompt: "Use ex:expand when the user explicitly invokes expand to give concept A more source-grounded detail and context-fit examples for clearer explanation."
policy:
  allow_implicit_invocation: false
