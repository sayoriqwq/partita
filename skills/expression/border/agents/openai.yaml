interface:
  display_name: "Border"
  short_description: "The user explicitly invokes border to separate two concepts by their core difference, either inferred from context or named by the user"
  default_prompt: "Use ex:border when the user explicitly invokes border to separate two concepts by their core difference, either inferred from context or named by the user."
policy:
  allow_implicit_invocation: false
