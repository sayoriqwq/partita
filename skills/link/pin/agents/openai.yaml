interface:
  display_name: "Pin"
  short_description: "The user explicitly asks to pin a GitHub repository into the current project as a git-subtree pin with sibling subtree contract, anchor, route, editor policy, update path, verify path, and import block"
  default_prompt: "Use lk:pin when the user explicitly asks to pin a GitHub repository into the current project as a git-subtree pin with sibling subtree contract, anchor, route, editor policy, update path, verify path, and import block."
policy:
  allow_implicit_invocation: false
