interface:
  display_name: "Pin"
  short_description: "把外部 authority 以可溯源 link 接入项目"
  default_prompt: "使用 pin 建立外部 authority 的 link contract，明确 locator、version anchor 或 unlocked、local path、update path、verify path，并只通过脚本或 CLI materialize。"
policy:
  allow_implicit_invocation: false
