interface:
  display_name: "Pin"
  short_description: "把外部仓库接入为 agent 可读取 source-entry 真源"
  default_prompt: "使用 pin 建立 source-entry contract，明确 upstream repo/branch/ref、local prefix、anchor、agent route、editor policy、ownership、update/verify command 和 import block，并通过 partita source 或 owning wrapper 验证。"
policy:
  allow_implicit_invocation: false
