interface:
  display_name: "Pin"
  short_description: "The user explicitly asks to add, update, inspect, or verify a GitHub repository as a Source Pin through Partita's approved git-subtree lifecycle"
  default_prompt: "Use lk:pin when the user explicitly asks to add, update, inspect, or verify a GitHub repository as a Source Pin through Partita's approved git-subtree lifecycle."
policy:
  allow_implicit_invocation: false
