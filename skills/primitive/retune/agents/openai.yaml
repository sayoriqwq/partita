interface:
  display_name: "Retune"
  short_description: "Tune an existing skill from observed drift"
  default_prompt: "Use pm:retune to correct this existing Partita skill from the observed case or structure audit."
policy:
  allow_implicit_invocation: true
