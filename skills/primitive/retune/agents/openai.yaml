interface:
  display_name: "Retune"
  short_description: "Patching an existing identity-valid OpenAI/Codex skill from an evidence-anchored patch case that exposes a stale local surface, including Partita source structure. In Partita landing, patches an existing valid Partita source skill without rewriting identity"
  default_prompt: "Use pm:retune when patching an existing identity-valid OpenAI/Codex skill from an evidence-anchored patch case that exposes a stale local surface, including Partita source structure. In Partita landing, patches an existing valid Partita source skill without rewriting identity."
policy:
  allow_implicit_invocation: false
