interface:
  display_name: "Notate"
  short_description: "Create a case-rooted skill from a real case"
  default_prompt: "Use pm:notate to create a case-rooted skill from this real case."
policy:
  allow_implicit_invocation: false
