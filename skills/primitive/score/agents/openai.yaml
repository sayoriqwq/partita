interface:
  display_name: "Score"
  short_description: "The user explicitly invokes score or asks to apply sayoriqwq-style Markdown writing preferences to Markdown docs"
  default_prompt: "Use pm:score when the user explicitly invokes score or asks to apply sayoriqwq-style Markdown writing preferences to Markdown docs."
policy:
  allow_implicit_invocation: false
