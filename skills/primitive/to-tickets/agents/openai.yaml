interface:
  display_name: "To Tickets"
  short_description: "The user explicitly invokes to-tickets to decompose one settled plan, specification, or conversation into approved vertical tracer tickets with explicit blocking edges"
  default_prompt: "Use pm:to-tickets when the user explicitly invokes to-tickets to decompose one settled plan, specification, or conversation into approved vertical tracer tickets with explicit blocking edges."
policy:
  allow_implicit_invocation: false
