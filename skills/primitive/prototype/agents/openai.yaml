interface:
  display_name: "Prototype"
  short_description: "The user explicitly invokes prototype to answer exactly one design question with the cheapest useful throwaway runnable artifact"
  default_prompt: "Use pm:prototype when the user explicitly invokes prototype to answer exactly one design question with the cheapest useful throwaway runnable artifact."
policy:
  allow_implicit_invocation: false
