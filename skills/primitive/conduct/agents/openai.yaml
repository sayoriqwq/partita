interface:
  display_name: "Conduct"
  short_description: "Creating a public workflow skill from an evidence-anchored workflow case with default failure, workflow pressure, recognition surface, orchestration action, and target runtime shape. Defaults to OpenAI/Codex skill target; in Partita landing creates a public workflow source skill"
  default_prompt: "Use pm:conduct when creating a public workflow skill from an evidence-anchored workflow case with default failure, workflow pressure, recognition surface, orchestration action, and target runtime shape. Defaults to OpenAI/Codex skill target; in Partita landing creates a public workflow source skill."
policy:
  allow_implicit_invocation: false
