# Gate

Use an interpretation gate when a user instruction can change global routing,
skill names, marker conventions, install behavior, or verifier policy.

State what will change, what will not change, and the runtime effect before
editing.
