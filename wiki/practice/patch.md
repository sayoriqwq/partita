# Patch

Patch an existing skill from a real recurrence.

Patch the smallest surface that prevents recurrence. If the case exposes a new
governance action, split instead of widening the old skill.

See [[skill/case/patch]] and [[projection/loss]].
