# Migrate

Migration replaces old surfaces with the new wiki state.

For this migration:

```text
rules/ removed
theory/ renamed into wiki/
design-v1 absorbed and deleted
skills/RESOLVER.md removed
```

Do not leave compatibility language unless explicitly required by runtime.
