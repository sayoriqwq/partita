# Rule

The minimal skill rule is:

```text
Facing A, first Y, to avoid X.
```

Where:

```text
A = case pattern
Y = governance action
X = default failure
```

The rule must trigger before the agent enters `X`.

See [[skill/case/pattern]], [[skill/governance/action]], and
[[harness/failure]].
