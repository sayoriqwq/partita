# Effects

Effects are the side effects a skill is allowed to produce.

Use these surfaces:

```text
conversation
filesystem
external
```

`none` must be explicit when a surface is not allowed. Effects cannot be wider
than the skill boundary.

See [[projection/runtime]].
