# Assertion

The minimal skill assertion is:

```text
Facing A, first Y, to avoid X.
```

Where:

```text
A = case pattern
Y = governance action
X = default failure
```

The assertion must trigger before the agent enters `X`.

See [[skill/case/pattern]], [[skill/governance/action]], and
[[harness/failure]].
