# Invocation

Invocation is whether the runtime may select a skill without an explicit user
handle.

Use implicit invocation only when accidental activation is low cost. Use
explicit invocation when a skill changes collaboration mode, persists across a
topic, or strongly narrows the work.

OpenAI metadata projects this through [[projection/codex/openai]].
