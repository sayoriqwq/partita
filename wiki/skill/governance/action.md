# Action

The governance action is the `Y` in [[skill/assertion]].

It is the smallest intervention that prevents the default failure. Multiple
steps are allowed only when they serve the same action.
