# Split

Split a skill when one node needs multiple governance actions.

Ask:

```text
Is the same default failure being prevented?
Is the same action being applied?
Can the same validation judge it?
```

If not, split and connect the nodes with [[skill/governance/family]].
