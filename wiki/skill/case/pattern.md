# Pattern

A case pattern is the reusable shape extracted from real cases.

It is not a task category, gate name, or keyword list. Keywords can help
runtime projection, but they do not define the pattern.

Patterns form the `A` in [[skill/rule]].
