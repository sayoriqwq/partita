# Pressure

Pressure is the default agent failure read from a case.

It is not a desired capability. A single case can establish pressure when it
shows a default failure rather than a one-off mistake.

Pressure becomes the `X` in [[skill/rule]].
