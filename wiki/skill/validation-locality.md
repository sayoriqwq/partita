# Validation Locality

Skill validation only checks whether the current skill action worked:

```text
A matched
Y applied
X avoided
boundary respected
effects respected
hard checks passed
```

It does not validate workflow completion. Gate exit belongs to
[[workflow/gate/exit]].
