# Marker

A marker is the runtime projection of a handle.

Partita currently uses `🧭` as the loaded-skill marker. The marker appears only
when a Partita skill is active.

See [[projection/codex/marker]].
