# Nodes

The verifier must check that required wiki nodes exist.

Node identity is the path under `wiki/`. Do not add separate `id`, `kind`,
`area`, or `status` fields until implementation needs them.
