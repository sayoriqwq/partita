# Package

Package checks ensure the shipped plugin contains only current surfaces.

The package must include `wiki/**` and must not include removed `rules/**`,
`theory/**`, `skills/RESOLVER.md`, or `design-v1.md`.
