# Workflow

A workflow is an ordered delivery path.

The current working model is:

```text
Idea -> Research -> Prototype -> PRD -> Kanban -> Execution -> QA
```

Some gates are optional, but skipped gates still leave asset questions for
downstream work.
