# Optional

An optional gate may be skipped, but its missing assets must be handled.

Downstream work must either receive the assets from the user, derive them from
existing material, or explicitly accept degraded confidence.
