# Behavior

Agent behavior is the observable way the agent acts before, during, and after a
task.

Partita governs behavior only where the unguided default is predictably wrong.
Always identify the default behavior before adding a skill or workflow rule.

See [[harness/failure]] and [[skill/case/pressure]].
