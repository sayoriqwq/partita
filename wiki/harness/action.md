# Action

A governance action is the smallest intervention that prevents a default
failure.

It must be concrete enough for runtime behavior and validation. If one node
needs several unrelated interventions, split it.

See [[skill/governance/action]].
