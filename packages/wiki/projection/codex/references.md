# References

`references` 承载 conditional runtime detail。

cases、long examples、style protocols 或 extended guidance SHOULD 放在 references 中。

every-run instructions 属于 `SKILL.md`。

runtime references MAY 是从 wiki source nodes 生成的 projections。

projected references MUST 能被 installed skill runtime 独立使用，并通过 [[projection/codex/projection-marker]] 检查。
