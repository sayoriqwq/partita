# Loss

`projection loss` 是 wiki intent 和 runtime material 之间的 semantic loss。

示例：

```text
generic description replaces a precise pattern
explicit invocation is projected as implicit
hard boundary has no verifier
references hide every-run instructions
```

care work 负责发现并修复 loss。
