# Context

`context` 是让 behavior governance 成立的有边界情境。

`context` 不是 memory bag。

它包括当前 user request、active workflow gate、已接受 constraints、loaded skill 和相关 durable decisions。

参见 [[workflow/gate/contract]] 和 [[collaboration/context]]。
