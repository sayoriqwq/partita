# Failure

`default failure` 是 agent 在没有 governance layer 时自然走向的失败路径。

MUST NOT 从 positive capability wish 直接定义 skill。

应该把 wish 重述为 default failure；如果无法重述，就不要创建 skill。

参见 [[skill/case/pressure]] 和 [[skill/rule]]。
