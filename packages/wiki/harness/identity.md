# Identity

`governance identity` 由 action 决定，不由 file、task name、gate 或 pressure family 决定。

一个稳定 action 可以跨 gate。

不同 actions 需要不同 nodes 或 skills。

参见 [[skill/governance/identity]] 和 [[skill/governance/split]]。
