# Validation

`validation` 检查 governance action 在当前 context 中是否有效。

validation 属于被治理对象的 local 判断。

skill validation 不替代 gate exit、package verification 或 global project quality。

参见 [[skill/validation-locality]] 和 [[workflow/gate/exit]]。
