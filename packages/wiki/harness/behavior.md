# Behavior

`agent behavior` 是 agent 在任务前、任务中和任务后的可观察行动方式。

Partita 只治理 unguided default 明确会错的行为。

新增 skill 或 workflow rule 前，MUST 先识别 default behavior。

参见 [[harness/failure]] 和 [[skill/case/pressure]]。
