# Assertion

`assertion` 是可以成立、过期、冲突、拆分、合并、被挑战或被接受的 independent semantic unit。

它可以是 fact、constraint、interpretation、assumption、requirement、definition 或 judgment。

在 conversation 中，`argue` 会在 unstable assertions 被用作 shared premises 前对其进行 pressure test。

`baseline` 记录 accepted assertions。
