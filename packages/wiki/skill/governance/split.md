# Split

当一个 node 需要多个 governance actions 时，MUST split skill。

检查问题：

```text
Is the same default failure being prevented?
Is the same action being applied?
Can the same validation judge it?
```

如果答案不是全部成立，MUST split，并用 [[skill/governance/family]] 连接 nodes。
