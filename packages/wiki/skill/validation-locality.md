# Validation Locality

`skill validation` 只检查当前 skill action 是否生效：

```text
A matched
Y applied
X avoided
boundary respected
effects respected
hard checks passed
```

它不验证 workflow completion。

gate exit 属于 [[workflow/gate/exit]]。
