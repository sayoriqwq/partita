# Duration

`duration` 是 skill 在 activation 后保持 active 的时间范围。

common durations：

```text
turn
task
topic
mode
gate span
```

每个 duration 都需要 close condition。

gate-based duration 由 [[workflow/gate/span]] 处理。
