# Pressure Reading

`pressure reading` 是把 case 连接到 recurring default failure 的 judgment。

需要回答的问题是：

```text
Without governance, where would the agent naturally slide in this class of situation?
```

如果无法回答这个问题，MUST NOT 创建 skill。
