# Boundary

`boundary` 定义什么属于一个 skill，什么必须 route elsewhere。

scope MUST 和 enforcement 分开。

soft boundaries 依赖 model judgment。

hard boundaries 需要 machine-checkable surface，例如 verifier、test、schema 或 CLI command。

参见 [[projection/verifier/shape]]。
