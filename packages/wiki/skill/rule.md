# Rule

最小 skill rule 是：

```text
Facing A, first Y, to avoid X.
```

其中：

```text
A = case pattern
Y = governance action
X = default failure
```

rule MUST 在 agent 进入 `X` 前触发。

参见 [[skill/case/pattern]]、[[skill/governance/action]] 和 [[harness/failure]]。
