# Primitive

`primitive skill` 承载一个 pressure 和一个 governance action。

它不是 folder shape、runtime export、plugin namespace、command wrapper 或 task category。

primitive skills 从 [[skill/rule]] 写出，并通过 [[skill/governance/identity]] 保持稳定。
