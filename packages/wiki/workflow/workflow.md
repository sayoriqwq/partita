# Workflow

`workflow` 是 ordered delivery path。

当前 working model 是：

```text
Idea -> Research -> Prototype -> PRD -> Kanban -> Execution -> QA
```

部分 gates 可以 optional。

被跳过的 gates 仍会给 downstream work 留下 asset questions。
