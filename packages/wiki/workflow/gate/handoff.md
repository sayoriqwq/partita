# Handoff

`handoff` 是一个 gate 把 assets 传给另一个 gate 的位置。

projection loss 经常在这里出现：accepted constraints、exclusions、source tiers 或 risks 会在 downstream work 中消失。

只有当同一个 governance action 保持稳定时，才使用 cross-gate guards。
