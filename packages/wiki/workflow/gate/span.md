# Span

`gate span` 是投影到 workflow gates 上的 skill lifecycle。

它定义 skill 可以在哪里 activate，以及何时 close。

它不证明 skill 的存在合理，也不允许 skill 跨 gates 改变 action。

参见 [[skill/duration]] 和 [[skill/governance/identity]]。
