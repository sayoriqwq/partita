# Audit

`audit` 在不发明 case 的前提下检查 semantic preservation。

audit 面包括 primitive identity、projection loss、runtime shape、metadata policy 和 verifier coverage。

参见 [[skill/governance/identity]] 和 [[projection/verifier/index]]。
