# Quality policy

Run `pnpm typecheck`, `pnpm lint --max-warnings 0`, and `pnpm verify`.
ESLint composes the stable Artifact export into target-owned executable config;
missing integration is a blocking planning Issue with Artifact guidance.
